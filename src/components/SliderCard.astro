---

export interface props {
    title: string;
    text: string;
    src: string;
}

const {title, text, src} = Astro.props;
---

<div class="flex flex-col items-center text-center h-48 w-600 ml-36 my-8 bg-wb-muted-mint rounded-lg p-8 hover:bg-wb-pale-peach">
    <h1 class="font-semibold mb-4">{title}</h1>
    <p>
        {text}
    </p>
</div>